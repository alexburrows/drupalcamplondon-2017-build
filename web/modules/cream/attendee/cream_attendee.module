<?php
/**
 * @file
 * Cream attendee functionality.
 */

/**
 * Implements hook_form_alter().
 * @param $form
 * @param \Drupal\Core\Form\FormStateInterface $form_state
 * @param $form_id
 */
function cream_attendee_form_alter(&$form, \Drupal\Core\Form\FormStateInterface $form_state, $form_id) {
  if ($form_id == 'user_register_form' && \Drupal::currentUser()->isAnonymous()) {
  }
}